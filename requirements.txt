# GhostPillage — Python Dependencies
# Install: pip3 install -r requirements.txt
# Kali Linux mein zyadaatar packages pre-installed hote hain

# ==================== CORE ====================
scapy>=2.5.0
netifaces>=0.11.0
requests>=2.31.0
psutil>=5.9.0
colorama>=0.4.6

# ==================== PARSING ====================
pyyaml>=6.0

# ==================== NETWORK ====================
# (scapy already covers most network operations)

# ==================== UTILITY ====================
# tqdm — progress bars (optional, for future use)
# tabulate — pretty table output (optional, for future use)